services:
  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    extra_hosts:
      - host.docker.internal:host-gateway
    ports:
      - ${NGINX_PORT:-80}:80
    restart: always
#    env_file:
#      - .env
